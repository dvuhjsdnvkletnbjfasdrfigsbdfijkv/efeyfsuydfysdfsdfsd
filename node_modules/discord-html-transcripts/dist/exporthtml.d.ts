import * as discord from 'discord.js';
import { internalGenerateOptions, ObjectType, ReturnTypes } from './types';
export declare const date: {
    options: Intl.DateTimeFormatOptions;
};
declare function generateTranscript<T extends ReturnTypes>(messages: discord.Message[], channel: discord.TextBasedChannel, opts?: internalGenerateOptions): ObjectType<T>;
export default generateTranscript;
